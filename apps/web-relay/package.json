{
  "name": "@relais/web-relay",
  "private": true,
  "version": "0.1.0",
  "description": "Relais — la page web du parcours du contact (E5, F4) : pour qui n'installe pas l'app. Embarque crypto-core et app-core ; tout le déchiffrement se fait dans le navigateur.",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "lint": "eslint src test",
    "test": "vitest run"
  },
  "dependencies": {
    "@relais/api-client": "0.1.0",
    "@relais/app-core": "0.1.0",
    "@relais/crypto-core": "0.1.0",
    "buffer": "^6.0.3"
  },
  "devDependencies": {
    "eslint": "^9.30.0",
    "typescript": "^5.8.3",
    "typescript-eslint": "^8.35.0",
    "vite": "^8.3.0",
    "vitest": "^3.2.4"
  }
}
