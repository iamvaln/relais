{
  "name": "relais",
  "private": true,
  "version": "0.1.0",
  "description": "Relais — passe le relais, pas le chaos.",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "api": "npm run --workspace apps/api",
    "dev": "npm run dev --workspace apps/api",
    "build": "npm run build --workspaces --if-present",
    "test": "npm run test --workspaces",
    "typecheck": "npm run typecheck --workspaces",
    "lint": "npm run lint --workspaces",
    "db:generate": "prisma generate --schema prisma/schema.prisma",
    "crypto": "npm run --workspace packages/crypto-core",
    "core:build": "npm run build --workspace packages/crypto-core --workspace packages/api-client --workspace packages/app-core --workspace packages/admin-core",
    "mobile": "npm run --workspace apps/mobile"
  },
  "devDependencies": {
    "prisma": "^6.19.0"
  }
}
