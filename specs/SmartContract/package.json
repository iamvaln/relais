{
  "name": "relais-contract",
  "version": "1.0.0",
  "description": "Smart contract dead man's switch pour Relais — Arbitrum One",
  "scripts": {
    "compile":  "hardhat compile",
    "test":     "hardhat test",
    "test:gas": "REPORT_GAS=true hardhat test",
    "deploy:testnet": "hardhat run scripts/deploy.js --network arbitrumSepolia",
    "deploy:prod":    "hardhat run scripts/deploy.js --network arbitrumOne",
    "clean":    "hardhat clean"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^5.0.0",
    "hardhat": "^2.22.0",
    "dotenv": "^16.0.0"
  }
}
